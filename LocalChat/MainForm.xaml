<Window x:Class="LocalChat.MainForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LocalChat"
        mc:Ignorable="d"
        Title="MainForm" Height="436" Width="750">
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="通信履歴" HorizontalAlignment="Left" Height="37" VerticalAlignment="Top" Width="95" FontSize="18"/>

        </Grid>
        <Grid Grid.Column="1" Grid.Row="0">
            <Label x:Name="ChatTitle" Content="" HorizontalAlignment="Left" Height="31" VerticalAlignment="Center" FontSize="18" Margin="10,0,0,0"/>
        </Grid>
        <Grid Grid.Column="0" Grid.Row="1">
            <Grid Margin="10,0,0,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Content="既存のチャット" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="PartnersList" >
                    </StackPanel>
                </ScrollViewer>
                <Button Grid.Row="2" Content="＋ 新規追加先　" HorizontalAlignment="Center" VerticalAlignment="Center" Click="OpenAddNewPartnerForm" Background="White"/>
                <Button Grid.Row="3" Content="設定" HorizontalAlignment="Left" VerticalAlignment="Top" Click="OpenConfigForm"/>
            </Grid>
        </Grid>
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid Margin="0,0,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="7*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ScrollViewer Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="DisplayMessage" >
                    </StackPanel>
                </ScrollViewer>
                <Grid Grid.Row="1" Grid.Column="0" Margin="10,0,0,10">
                    <TextBox TextWrapping="Wrap" Text="[新しいメッセージの入力]" IsEnabled="False"/>
                    <TextBox x:Name="MessageText" TextWrapping="Wrap" Text="" TextChanged="MessageText_TextChanged" Background="{x:Null}"/>

                </Grid>

                <Button Grid.Row="1" Grid.Column="1" Content="送信ボタン" Click="Send_Click" RenderTransformOrigin="0,0.52" Margin="0,0,10,10"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
